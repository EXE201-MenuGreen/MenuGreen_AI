# Runtime Models

Place ONNX intent model bundle here:

- `intent_onnx/model.int8.onnx` or `intent_onnx/model.onnx`
- `intent_onnx/label_config.json`
- tokenizer files (`tokenizer.json`, `tokenizer_config.json`, `special_tokens_map.json`, vocab files)
